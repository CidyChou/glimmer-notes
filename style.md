# 拾光笔记视觉规范

本文档是拾光笔记当前界面的视觉与交互基线。新增组件应优先复用这里的颜色、形状、间距和反馈方式，再考虑扩展。

## 设计方向

- 产品类型：移动端优先的灵感捕捉与整理工具
- 视觉语言：克制、空间感、轻科幻，并提供暗色与亮色环境
- 品牌印象：快速、安静、有生命力
- 设计变化度：5 / 10
- 动效强度：3 / 10
- 信息密度：6 / 10
- 实现方式：Taro + React + CSS variables + 原生 CSS

视觉设计采用定向优化策略。保留既有的信息架构、中文语气与空间感，通过“荧光夜”“暮光紫”和亮色“晨光纸”提供不同的环境与品牌强调色。

## 核心原则

1. 先捕捉，再整理。记录动作必须比分类动作更直接。
2. 一屏只保留当前皮肤的一个主强调色。普通操作使用皮肤中性色，主操作使用皮肤强调色。
3. 动效必须表达状态变化、触控反馈或归类结果。
4. 手机端所有主要点击区域不小于 44px。
5. 组件层级依靠对比度、内边框和间距，不依靠大面积外发光。
6. 文案直接说明功能，不使用装饰性版本号、步骤编号和模糊比喻。

## 颜色

所有可见颜色必须由 `src/theme` 中的 `ThemeDefinition` 提供。组件 CSS 使用语义化变量，Canvas、优先级和灵感球使用同一主题对象；不得在组件中新增独立十六进制或 RGBA 色值。

### 皮肤

| Token | 荧光夜 | 暮光紫 | 晨光纸 |
| --- | --- | --- | --- |
| `page-bg` | `#07090d` | `#0c0914` | `#f7f5ef` |
| `stage-bg` | `#020304` | `#05040a` | `#e7e5df` |
| `surface-low` | `#0d1016` | `#12101c` | `#eceae3` |
| `surface` | `#141820` | `#191625` | `#ffffff` |
| `surface-high` | `#191d26` | `#211d30` | `#f3f0e8` |
| `text-primary` | `#f0f2f6` | `#f6f2ff` | `#1c2430` |
| `text-secondary` | `#aeb5c1` | `#c5bdd5` | `#465263` |
| `text-muted` | `#858e9d` | `#968da8` | `#647083` |
| `text-disabled` | `#626a77` | `#6d647c` | `#89919e` |
| `accent` | `#dcfa66` | `#a78bfa` | `#4f63d8` |
| `on-accent` | `#0a0c0d` | `#100b1c` | `#ffffff` |

以下基础色表描述默认“荧光夜”皮肤，其他皮肤保持相同语义层级。

### 基础色

| Token | 色值 | 用途 |
| --- | --- | --- |
| `page-bg` | `#07090d` | 页面主背景 |
| `stage-bg` | `#020304` | 桌面预览背景 |
| `surface-low` | `#0d1016` | 输入框、列表与底层面板 |
| `surface` | `#141820` | 普通控件背景 |
| `surface-high` | `#191d26` | 底部弹层与高层级面板 |
| `border-soft` | `rgba(255,255,255,.06)` | 普通分隔与卡片边界 |
| `border-strong` | `rgba(255,255,255,.095)` | 弹层与重点容器边界 |

### 文本色

| Token | 色值 | 用途 |
| --- | --- | --- |
| `text-primary` | `#f0f2f6` | 标题、输入内容 |
| `text-secondary` | `#aeb5c1` | 标签、重要辅助信息 |
| `text-muted` | `#858e9d` | 说明文字 |
| `text-disabled` | `#626a77` | 禁用状态 |

### 强调色与语义色

| Token | 色值 | 用途 |
| --- | --- | --- |
| `accent` | `#dcfa66` | 主按钮、激活导航、输入光标 |
| `urgent` | `#ff5b61` | 现在做 |
| `important` | `#ffab58` | 计划做 |
| `quick` | `#69a8ff` | 快处理 |
| `inbox` | `#969eae` | 碎片池 |

优先级颜色只用于分类语义，不作为普通 CTA 或装饰色。每套皮肤必须提供七槽灵感球色板，同一想法在不同皮肤中使用相同槽位。

### 皮肤切换

- 首页顶栏设置按钮进入独立设置页。
- 皮肤卡片点选后立即生效，并在本机持久保存。
- 选中状态必须同时显示勾选和“使用中”，不能只依赖颜色。
- 主题切换只改变颜色和光影，不改变排版、布局和信息层级。

## 字体与排版

- 字体栈：`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "PingFang SC", "Microsoft YaHei", sans-serif`
- 页面标题：24px，字重 780，紧凑字距
- 弹层标题：17px，字重 790
- 输入正文：16px，字重 500，行高 1.55
- 移动端列表标题：16px，字重 680，行高 1.35
- 移动端列表摘要：13px，单行显示，超出宽度使用省略号
- 分类标签与普通按钮：12-14px，字重 720 以上
- 时间、状态和操作提示：不小于 11px，保证与背景有清晰对比
- 仅装饰性微标签可以使用 10px；核心信息禁止使用 10px 以下字号
- 数字计数使用 `tabular-nums`，减少输入时的视觉跳动

英文品牌名可以使用紧凑字距。中文正文保持自然字距，不使用大段全大写或过宽字距。

## 间距

基础间距单位为 4px，常用组合如下：

- 紧凑间距：4px、6px、8px
- 组件内部间距：10px、12px、14px
- 区块间距：16px、18px、24px
- 手机页面左右安全边距：14-18px
- 底部固定控件：基础距离 12px，再叠加 `env(safe-area-inset-bottom)`

## 圆角规则

- 小型状态控件：10-14px
- 输入框、列表卡片、普通按钮：16-19px
- 底部导航与大型面板：24-26px
- 圆形节点、分类圈和状态点：50%
- 胶囊形只用于短提示或极小状态，不用于所有按钮

同层级组件必须使用同一档圆角。主按钮与输入框可以采用相邻档位，形成明确的操作层级。

## 阴影与材质

- 面板使用与皮肤一致的渐变、1px 半透明边框和轻微内高光；亮色皮肤使用独立深色边框根值，避免白色表面失去层级。
- 外阴影使用背景同色系，不使用纯黑硬阴影；亮色皮肤使用低饱和蓝灰阴影。
- 品牌色光效只用于节点、激活状态和拖拽目标。
- 底部导航采用 Liquid Glass 的 Web 近似实现，由当前皮肤半透明介质、折射边缘、局部镜面反光和背景饱和度共同构成。
- 玻璃外圈使用低透明白光，顶部高光强于底部；反光只占局部，不能均匀铺满整个面板。
- 光学边缘必须具有明显的明暗节奏，只在左上、中央拱冠和少量转折位置出现亮点，其余边缘保持低亮度。
- 玻璃外圈采用清晰矢量内缘与低透明柔光外晕两层结构，保证缩放后仍然锐利，不能依靠模糊描边代替轮廓。
- 最外层光泽使用左上白色镜面光、全圈柔光和右下环境色回光构成，并与玻璃主体复用同一个轮廓蒙版。
- 外层玻璃使用约 1 到 2px 的光学折射边缘配合低透明柔光，禁止使用粗边框或大面积灰白边带模拟厚度。
- 暗色皮肤的玻璃内部以深色通透介质为主；亮色皮肤使用浅冷白透明介质，并保持接近页面表面的明度。反光集中在局部边缘，禁止使用高不透明灰白填充把整个底座铺亮。
- 深色介质通过透明暗层和背景亮度控制加深，仍需保留环境色透入，不能退化成纯黑不透明面板。
- Liquid Glass 的强度通过局部镜面高光、环境色折射和背景透出度提升，不通过增加整块白色填充实现。
- 底座和中央拱冠必须由同一个轮廓蒙版生成，复用同一次模糊与同一条高光边缘，不使用独立圆形或连接层遮缝。
- 玻璃效果是 Web 近似实现，必须提供不透明背景作为降级方案。
- `prefers-reduced-transparency` 下关闭背景模糊。

## 输入组件

### 项目级控件基线

项目级尺寸 token 统一定义在 `src/styles/design-system.css`，颜色 token 继续由 `src/theme` 提供。新组件与重构组件禁止复制一套新的输入框和按钮数值。

| Token | 当前值 | 用途 |
| --- | --- | --- |
| `--control-height-md` | `48px` | 普通输入框与相邻按钮高度 |
| `--control-radius-md` | `14px` | 普通输入框与相邻按钮圆角 |
| `--control-padding-inline` | `14px` | 输入框水平内边距 |
| `--control-gap` | `8px` | 同一表单行内控件间距 |
| `--control-font-size` | `16px` | 输入内容与 placeholder，避免移动端浏览器聚焦缩放 |
| `--control-action-font-size` | `13px` | 普通操作按钮文字 |

表单实现必须遵守以下规则：

1. 输入框和同一行按钮必须使用相同高度、圆角和垂直对齐方式。
2. Taro H5 的 `Input` 外层高度不会自动传给内部原生 `<input>`。页面样式必须让内部 `.weui-input` 使用 `height: 100%`，并同步行高，不能只设置 `taro-input-core` 外层。
3. 输入内容、placeholder 和按钮文字必须在控件的光学中心，不能依赖浏览器默认行高碰巧对齐。
4. 普通启用按钮使用 `accent` 背景和 `on-accent` 文字；禁用按钮使用 `surface-disabled` 背景和 `text-disabled` 文字，不通过整块低透明度制造禁用状态。
5. Taro 的条件禁用属性必须写成 `disabled={enabled ? undefined : true}`。禁止传入 `false`，避免 H5 生成 `disabled="false"` 后仍被 CSS 属性选择器判定为禁用。
6. 输入框使用可见标签与 `ariaLabel`，placeholder 只提供示例；跨端 placeholder 颜色通过 `placeholderClass` 统一。
7. 聚焦状态只增强边框和细聚焦环，不改变控件尺寸。
8. H5 和微信小程序都必须验证空值、输入中、可提交、提交成功和重复值反馈状态。

新增想法弹层采用“捕捉舱”结构：

1. 顶部显示明确标题和一句简短说明。
2. 输入标签位于 textarea 上方，placeholder 不承担标签职责。
3. 聚焦时只增强边框、内层背景和细小聚焦环。
4. 输入框下方显示帮助信息与实时字数。
5. 空内容时保存按钮保持可见但不可用。
6. 底部明确显示保存目标为“碎片池”。
7. 主按钮基准高度为 52px，确保 320px 窄屏缩放后仍保留约 44px 的触控高度。

输入文案应降低开始记录的心理负担。推荐使用“写下此刻冒出来的想法...”这类直接提示。

项目选择支持在任务详情弹层内直接新增项目：使用带虚线边框的“新增项目”入口，展开名称输入、颜色选择和保存按钮。创建成功后立即加入项目列表，并自动应用到当前任务；同名项目保留输入状态并显示提示。

## 样式工具链

当前项目不直接引入 Tailwind v4。原因不是 Tailwind 无法使用，而是本项目同时输出 H5 和微信小程序，并保留 iOS 12 浏览器目标；直接加入 Web 侧 Tailwind 会形成两套行为不同的样式链路。

当前规范方案如下：

1. 主题颜色使用 `src/theme` 的语义变量。
2. 尺寸、圆角、控件间距与动效时长使用 `src/styles/design-system.css`。
3. 页面 CSS 负责结构和组件状态，禁止在 JSX 中堆叠任意值模拟 utility class。
4. 相同控件优先抽成组件或复用同一组 class，不能只复制 CSS。
5. 新增 token 前先检查现有 token 能否表达，不因单个页面增加近似数值。

如果未来迁移 Tailwind，应作为独立工程改造完成：明确升级浏览器基线，使用适配 Taro Webpack 5 的小程序转换插件，关闭小程序端 preflight，分别完成 H5 与微信端视觉回归，再逐步迁移已有组件。迁移期间不能同时维护 Tailwind、旧页面私有数值和另一套组件库三套系统。

## 底部导航

- “空间”和“整理”是同级主导航。
- 激活项使用当前皮肤强调色背景和 `on-accent` 文字。
- 导航采用一体式悬浮玻璃底座，两侧入口使用独立胶囊形，材质应有实色回退。
- 中央新增按钮使用 70px 圆形主操作，不使用独立白色描边或圆形托座。
- 中央拱冠属于玻璃底座自身，外轮廓必须连续，不能在按钮周围形成第二个完整圆环。
- 玻璃底座基准高度为 76px，两侧导航胶囊基准高度为 54px，可见宽度不超过 126px。
- 中央加号图形使用 24px，加号圆底边必须与两侧导航胶囊底边对齐。
- 拱冠圆弧只绘制到底座上沿，不能延伸进底座内部形成接缝。
- 拱冠相对底座上沿的凸出高度约为 18px，避免形成高耸的半圆托座。
- 拱冠与底座使用单一路径连续成形，中央区域不得存在底座自身的水平上边线。
- 玻璃底座外沿需要保留克制的白色折射光，顶部与左上区域允许出现局部镜面反光。
- 按压反馈使用 `scale(.955)` 左右，加号同步旋转，不改变布局尺寸。
- 导航必须叠加底部安全区，不能贴住手机浏览器工具栏。

## 整理与拖拽

- 四个状态 tab 固定在列表下方，仅通过点击切换，禁用左右滑动切页。
- 列表右侧使用 2 x 3 六点手柄，触控区域至少 44px。
- 拖拽无长按等待：手指移动约 5px 即启动；卡片主体优先识别纵向滚动，横向/斜向启动拖拽。
- 点击六点手柄打开快速归类；拖动手柄或卡片则立刻进入拖拽。
- 拖拽副本为跟随想法颜色的光点（约 14px 核心 + 柔光晕），不得保持卡片矩形，不显示想法文字。
- 启动瞬间源卡片缩小淡出，光点从凝聚动画进入跟手飞行；靠近目标时轻微放大发光（磁吸感）。
- 空间页与整理页必须复用同一套圆形收纳目标，不得出现矩形分类按钮。
- 收纳圈中心使用 20px 实心色彩核心，并通过同色阴影表达发光与吸附状态。
- 拖拽时只出现当前分类之外的三个收纳圈。
- 松手淡出期间必须冻结当前三个目标，不能短暂回退为四个分类。
- 当前目标通过位移、缩放和边界强化表达吸附状态。
- 拖拽时降低其他卡片的视觉权重。
- 松手后立即更新分类并显示明确 toast。

## 动效

| 场景 | 时长 | 说明 |
| --- | --- | --- |
| 按压 | 120-160ms | 缩放反馈 |
| 聚焦 | 180-220ms | 边框与背景过渡 |
| 页面切换 | 200-260ms | 透明度与小距离位移 |
| 弹层进入 | 250ms | 使用 `cubic-bezier(.16,1,.3,1)` |
| 拖拽吸附 | 140-180ms | 缩放与位移 |

只动画 `transform`、`opacity`、颜色和阴影。所有持续动效必须在 `prefers-reduced-motion: reduce` 下停止。

## 响应式与安全区

- 页面高度优先使用 `100dvh`，旧浏览器回退到 `100vh`。
- H5 viewport 必须包含 `viewport-fit=cover`。
- 320px 宽度下仍需保持 44px 的主要触控尺寸。
- 页面不可出现横向滚动。
- 固定弹层关闭时使用 `visibility: hidden` 和 `pointer-events: none`，避免扩大可滚动区域或拦截点击。

## 层级

| 层级 | z-index | 内容 |
| --- | --- | --- |
| 内容 | 3 | 空间画布、整理页 |
| 顶栏 | 12 | 品牌、搜索与设置按钮 |
| 底部导航 | 18 | 模式导航与新增按钮 |
| 搜索 | 25 | 搜索输入层 |
| 遮罩 | 30 | 弹层背景遮罩 |
| 弹层 | 31 | 新增想法、详情 |
| 提示 | 60 | toast |
| 拖拽浮层 | 70 | 拖拽中的卡片 |

新增层级前先判断是否能复用现有层级，避免随意增加 z-index。

## 可访问性

- 正文与背景至少达到 WCAG AA 对比度。
- 输入框必须有可访问名称，不能只依赖 placeholder。
- 禁用按钮需要同时通过颜色和 `disabled` 属性表达。
- 手柄、分类标签和收纳圈需要明确的 aria label。
- 重要操作必须支持触控，不能只依赖 hover。
- 尊重 `prefers-reduced-motion` 与 `prefers-reduced-transparency`。

## 文案规则

- 使用简短、具体的中文动作词，例如“收进来”“取消”“保存至”。
- 不使用装饰性版本号、步骤编号或模糊状态词。
- 页面可见文案不使用 em dash 或 en dash。
- 中间点只在确实表达元数据关系时使用，不能作为默认分隔符。
- 一条信息只表达一个动作或状态。

## 设计检查清单

- [ ] 一屏是否只使用当前皮肤的一个主强调色
- [ ] 新增颜色是否进入统一主题定义而非组件文件
- [ ] 两套皮肤是否覆盖页面、弹层、Canvas、优先级和灵感球
- [ ] 分类语义色是否只用于分类
- [ ] 主触控区是否至少 44px
- [ ] 输入框是否有标签、帮助信息和聚焦状态
- [ ] 主按钮是否有可见的禁用和按压状态
- [ ] 底部控件是否适配安全区
- [ ] 动效是否表达反馈或状态变化
- [ ] 是否支持 reduced motion 和 reduced transparency
- [ ] 页面是否没有横向溢出
- [ ] 可见文案是否清晰且没有特殊长破折号
